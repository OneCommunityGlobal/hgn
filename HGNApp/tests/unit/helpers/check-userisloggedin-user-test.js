
import { checkUserisloggedinUser } from 'hgnapp/helpers/check-userisloggedin-user';
import { module, test } from 'qunit';

module('Unit | Helper | check userisloggedin user');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = checkUserisloggedinUser([42]);
  assert.ok(result);
});

