
import { actionItemsHelper } from 'hgnapp/helpers/action-items-helper';
import { module, test } from 'qunit';

module('Unit | Helper | action items helper');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = actionItemsHelper([42]);
  assert.ok(result);
});

